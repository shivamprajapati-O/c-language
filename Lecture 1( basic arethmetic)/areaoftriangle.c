#include <stdio.h>
int main()
{
    float b , h ;
    printf("Enter the base of the triangle :- ");
    scanf("%f",&b);
    printf("Enter the hieght of the traingle - ");
    scanf("%f",&h);
    printf("Area of triangle is - %.2f", 0.5*b*h);
    
    return 0;
}